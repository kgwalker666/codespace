server:
  port: 9000
spring:
  datasource:
    url: jdbc:mysql://192.168.80.128:3306/cloud_user
    username: root
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver
  application:
    name: user-service
mybatis-plus:
  global-config:
    db-config:
      table-prefix: tb_
eureka:
  client:
    service-url:
      # 指定默认的区域
      defaultZone: http://127.0.0.1:7000/eureka